interface:
  display_name: "Completed Change Walkthrough"
  short_description: "Explain change actors for VS Code Markdown Preview"
  default_prompt: "Use $user-review-helper to explain this completed change actor-first, then export the walkthrough as portable Markdown with a standalone SVG diagram when useful."
