interface:
  display_name: "Architecture Review Walkthrough"
  short_description: "Adaptive instance-first walkthrough before reading code"
  default_prompt: "Use $user-review-helper to explain this change instance by instance at my current level, reset from missing prerequisites if I get lost, then walk me through one concrete code flow at a time."
