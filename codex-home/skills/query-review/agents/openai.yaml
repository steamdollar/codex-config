interface:
  display_name: "Query Review"
  short_description: "Explain and validate SQL semantics"
  default_prompt: "Use $query-review to explain this SQL top-down and check whether it matches the intended metric."
