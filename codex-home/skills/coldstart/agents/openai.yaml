interface:
  display_name: "Cold Start Handoff"
  short_description: "Continue the exact task in a fresh tab"
  default_prompt: "Use $coldstart to hand off the exact current task to a fresh tab without switching scope."
